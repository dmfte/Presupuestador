<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".FinTrackApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Presupuestador">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Presupuestador">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FileProvider for sharing PDF exports -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- Disable default WorkManager initialization so we can use HiltWorkerFactory -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="remove" />

        <!-- Widget entry activity — opened when user taps the amount button on a widget -->
        <activity
            android:name=".widget.WidgetEntryActivity"
            android:exported="false"
            android:theme="@style/Theme.Presupuestador"
            android:windowSoftInputMode="stateAlwaysVisible" />

        <!-- Receives the amount broadcast from WidgetEntryActivity -->
        <receiver
            android:name=".widget.WidgetAmountReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.tuapp.fintrack.WIDGET_AMOUNT_ENTERED" />
            </intent-filter>
        </receiver>

        <!-- Small entry widget -->
        <receiver
            android:name=".widget.SmallEntryWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_small_provider" />
        </receiver>

        <!-- Medium entry widget with period summary -->
        <receiver
            android:name=".widget.MediumEntryWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_medium_provider" />
        </receiver>

    </application>

</manifest>
